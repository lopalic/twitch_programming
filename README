Requirements

1. Have Ruby installed
2. setup BOT_USERNAME, CHANNEL and BOT_OAUTH_TOKEN environment variables

Setup

1. execute ruby twitch_listener.rb

Testing/development

1. run bundle config set path '.bundle'
2. run bundle install
3. run rubocop and rspec using bundle exec rubocop and bundle exec rspec
