1. introduce module for writing into vim
2. replace !write
  add - !prepend, !append, !rmline
  automatically center the last edited line?
3. secure the shell so nothing bad can happen
4. write the !test module which will actually make this usable
